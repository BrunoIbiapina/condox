cat > README.md << 'EOF'
# CondoX
Projeto Django do condomínio (portal, reservas, financeiro, comunicados, assembleias).

## Rodando local
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver